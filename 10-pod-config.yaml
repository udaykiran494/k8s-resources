apiVersion: v1
kind: Pod
metadata:
  name: config-map-pod
spec:
  containers:
  - name: nginx
    image: nginx
    envFrom:
    - configMapRef:
        name: pod-config
      

# apiVersion: v1
# kind: Pod
# metadata:
#   name: config-map-pod
# spec:
#   containers:
#   - name: nginx
#     image: nginx
#     envFrom:
#     - configMapRef:
#         name: pod-config